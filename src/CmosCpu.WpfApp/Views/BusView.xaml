<UserControl x:Class="CmosCpu.WpfApp.Views.BusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#2D2D2D" BorderBrush="#3E3E3E" BorderThickness="1" Padding="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Bus Log" FontWeight="Bold" Foreground="#569CD6" Margin="0,0,0,4" />

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
                <Button Content="Clear" Command="{Binding ClearLogCommand}" Margin="2" Padding="6,1" />
            </StackPanel>

            <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Entries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="18">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="60" />
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Cycle}" Foreground="#808080" FontSize="11" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{Binding Operation}" Foreground="#CE9178" FontSize="11" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2" Text="{Binding Address}" Foreground="#DCDCAA" FontSize="11" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="3" Text="{Binding Value}" Foreground="#DCDCAA" FontSize="11" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="4" Text="{Binding Device}" Foreground="#569CD6" FontSize="11" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
